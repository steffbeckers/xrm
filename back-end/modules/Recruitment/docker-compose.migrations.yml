version: '3.4'

services:
  migrations:
    build:
      context: ../../
      dockerfile: templates/service/database/Dockerfile
    depends_on:
      - sqlserver
    environment:
      - IdentityServer_DB=Recruitment_Identity
      - Recruitment_DB=Recruitment_ModuleDb
      - SA_PASSWORD=yourStrong(!)Password
